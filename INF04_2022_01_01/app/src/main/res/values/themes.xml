<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/zolty</item>
        <item name="colorPrimaryDark">@color/zolty</item>
        <item name="colorAccent">@color/niebieski</item>
        <item name="colorButtonNormal">@color/niebieski</item>
        <item name="android:buttonStyle">@style/customButton</item>
    </style>

    <style name="customButton" parent="Widget.AppCompat.Button" >
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="SecondAppTheme" parent="AppTheme" />

</resources>